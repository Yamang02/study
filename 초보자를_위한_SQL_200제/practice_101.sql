SELECT
    *
FROM
    EMP;

FLASHBACK TABLE EMP TO BEFORE DROP;

DROP TABLE EMP;

ROLLBACK;

SELECT
    ORIGINAL_NAME,
    DROPTIME
FROM
    USER_RECYCLEBIN;
    
    
/*복구할 테이블명을 다른 이름으로 변경도 가능 */

FLASHBACK TABLE EMP TO BEFORE DROP RENAME TO EMP2;